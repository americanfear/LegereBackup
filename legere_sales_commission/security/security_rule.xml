<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <record id="commission_comp_rule" model="ir.rule">
            <field name="name">Commission Company Rule</field>
            <field name="model_id" ref="model_commission_commission"/>
            <field name="domain_force">['|',('company_id','=',False),('company_id', 'in', company_ids)]</field>
        </record>

        <record id="commission_rules_comp_rule" model="ir.rule">
            <field name="name">Commission Rules Company Rule</field>
            <field name="model_id" ref="model_commission_rule"/>
            <field name="domain_force">['|',('company_id','=',False),('company_id', 'in', company_ids)]</field>
        </record>

        <record id="commission_allocation_comp_rule" model="ir.rule">
            <field name="name">Commission Allocation Company Rule</field>
            <field name="model_id" ref="model_commission_allocation"/>
            <field name="domain_force">['|',('company_id','=',False),('company_id', 'in', company_ids)]</field>
        </record>

        <record id="sale_commission_comp_rule" model="ir.rule">
            <field name="name">Sale Commission Company Rule</field>
            <field name="model_id" ref="model_sale_commission"/>
            <field name="domain_force">['|',('company_id','=',False),('company_id', 'in', company_ids)]</field>
        </record>

        <record id="sale_commission_line_comp_rule" model="ir.rule">
            <field name="name">Sale Commission Line Company Rule</field>
            <field name="model_id" ref="model_sale_commission_line"/>
            <field name="domain_force">['|',('company_id','=',False),('company_id', 'in', company_ids)]</field>
        </record>
    </data>
</odoo>