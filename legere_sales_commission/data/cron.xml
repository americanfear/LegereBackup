<odoo>
    <record id="generate_monthly_sales_commission_cron" model="ir.cron">
        <field name="name">Generate Monthly Sales Commission</field>
        <field name="model_id" ref="model_sale_commission_line"/>
        <field name="state">code</field>
        <field name="code">model.generate_monthly_sales_commission()</field>
        <field name="active" eval="True"/>
        <field name="interval_number">1</field>
        <field name="interval_type">months</field>
        <field name="numbercall">-1</field>
        <field name="nextcall" eval="(DateTime.now() + relativedelta(day=1, months=1)).strftime('%Y-%m-%d 04:00:00')" />
    </record>
</odoo>