<?xml version="1.0"?>
<odoo>
    <data>
        <template inherit_id="payment.checkout" id="checkout" name="Payment Checkout">
            <label t-if="show_tokenize_input[provider.id]" position="replace">
                <label>
                    <input name="o_payment_save_as_token" type="checkbox"/>
                    Save my payment details
                </label>
            </label>
        </template>
    </data>
</odoo>